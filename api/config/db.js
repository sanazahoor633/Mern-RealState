// import dotenv from "dotenv";
// dotenv.config();
// import mongoose from "mongoose";


// const conn = mongoose.connect(process.env.MONGO_URL, {}).then(()=>{console.log(`db connect with ${MONGO_URL}`);
